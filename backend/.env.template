# Required: get your key from https://aistudio.google.com
GEMINI_API_KEY=your_api_key_here

# Environment: "development" or "production" (default: production)
# Set to "development" locally to allow localhost CORS origins
APP_ENV=development

# Optional: add an extra allowed CORS origin (e.g. a staging frontend URL)
# FRONTEND_URL=https://your-staging-app.web.app
