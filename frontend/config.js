// Production API config — loaded by index.html when served from Firebase Hosting.
// Local development (localhost / file://) auto-targets http://127.0.0.1:8000 in app.js.
window.RFP_MATCH_CONFIG = {
    API_URL: "https://peopleos-rfp-response-assistant.onrender.com/match",
    ENV: "production"
};
