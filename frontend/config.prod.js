// Production config — points to deployed Render backend
// This is the canonical production file. config.js should always match this.
window.RFP_MATCH_CONFIG = {
    API_URL: "https://peopleos-rfp-response-assistant.onrender.com/match",
    ENV: "production"
};
